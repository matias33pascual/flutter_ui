import 'package:flutter/material.dart';

class Example extends StatelessWidget {
  final int exampleNumber;

  const Example({required this.exampleNumber, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
